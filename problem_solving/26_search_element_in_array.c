#include<stdio.h>
#define MAX_SIZE 100

void check_number(char array[]) {
  int input_num;
  int index_num = array[] - '0';

  for(int i = 0; i < MAX_SIZE; i++) {
  printf("Enter the array size:- \n");
  scanf("")
    if (index_num == input_num) {
      printf("The input number is in the array !!!\n");
    } else {
      printf("Not in the array !!!\n");
    }
  }
}

int main() {
  char array[MAX_SIZE];

  printf("Please enter the array size:- \n");
}
